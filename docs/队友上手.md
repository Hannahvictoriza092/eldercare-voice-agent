# 队友上手（4 步，10 分钟）

> 这份是给还没动过手的队友看的，**只讲怎么开始，不扯原理**。
> 遇到问题再去看 `CONTRIBUTING.md` 和 `docs/网络与Git配置.md`。

---

## 第 1 步：配 SSH（不做这步推不上去）

国内直连 GitHub 会失败，**必须用 SSH**。打开 PowerShell 依次执行：

```powershell
# 生成密钥（一路回车）
ssh-keygen -t ed25519 -C "你的邮箱" -f "$env:USERPROFILE\.ssh\id_ed25519" -N '""'

# 显示公钥，把输出的那一整行复制下来
Get-Content "$env:USERPROFILE\.ssh\id_ed25519.pub"
```

然后：

1. 打开 👉 https://github.com/settings/keys
2. 点 **New SSH key**
3. Title 随便填，Key type 选 **Authentication Key**
4. Key 框里粘贴刚才复制的那一行（**是 `.pub` 文件的内容，不是私钥！**）
5. 点 **Add SSH key**

验证一下：

```powershell
ssh -T git@github.com
```

看到 `Hi 你的用户名!` 就成功了。

> ❌ 报 `Permission denied` = 公钥没贴对
> ❌ 连接超时 = 看 `docs/网络与Git配置.md` 第四节的 443 端口方案

---

## 第 2 步：下载代码并跑通

```powershell
git clone git@github.com:Hannahvictoriza092/eldercare-voice-agent.git
cd eldercare-voice-agent
pip install -r requirements.txt

python -m pytest      # 应该看到 55 passed
python demo.py        # 看一眼整条链路长什么样
```

**跑不通就先别往下走**，在群里问。

---

## 第 3 步：找到你要写的文件

| 你负责 | 你的文件夹 | 先读 |
|---|---|---|
| 呼救 | `skills/emergency_call/` | 该文件夹下的 `README.md` |
| 健康和照顾反馈 | `skills/health_report/` | 该文件夹下的 `README.md` |

**你只在这个文件夹里写代码。** `common/` 是三个人共用的，要改先在群里说。

---

## 第 4 步：日常就这样干

```powershell
# ① 开工前先拉最新（最重要！）
git pull

# ② 写代码……

# ③ 提交前必须跑测试，全绿才能提交
python -m pytest

# ④ 提交推送
git add -A
git commit -m "feat(emergency): 实现发起呼救"
git push
```

**就这 4 条命令。** 不用建分支，直接推 `main`。

### push 被拒绝了？

```
! [rejected]  main -> main (fetch first)
```

这是正常的，说明有人比你先推了。照做：

```powershell
git pull
git push
```

### 提交信息怎么写

```
<类型>(<模块>): <做了什么>
```

```powershell
git commit -m "feat(emergency): 实现发起呼救"
git commit -m "fix(health_report): 修复周报统计错误"
git commit -m "docs(emergency): 补充 README"
```

类型用：`feat`（新功能）/ `fix`（修 bug）/ `docs`（文档）。

---

## 附：让系统认出你的 skill

写完后，在你的 `__init__.py`（比如 `skills/emergency_call/__init__.py`）里加两行：

```python
from .skill import EmergencyCallSkill
SKILL = EmergencyCallSkill()
```

然后验证：

```powershell
python -c "import skills; print(skills.all_skills()); print('跳过:', skills.skipped_packages())"
```

你的 skill 应该出现在 `all_skills()` 里，不该在 `跳过` 里。

> ⚠️ 注意 `SKILL = EmergencyCallSkill()` **有括号**（是实例，不是类）。
> 漏了括号会报错，报错信息里会提醒你。

**没写这两行之前，你的文件夹会被安静跳过，不会影响别人**——所以你可以放心地先建空文件夹、慢慢写。

---

## 卡住了怎么办

| 情况 | 看哪 |
|---|---|
| 推送失败 | `docs/网络与Git配置.md` 的第七节（自查清单） |
| 不懂 skill 该怎么写 | 抄 `skills/medication_reminder/`，那是完整的样板 |
| 接口/字段的约定 | `docs/接口约定.md` |
| 冲突了 | `CONTRIBUTING.md` 第五节 |
| 还是不行 | 群里问，把你看到的报错整段发出来 |
